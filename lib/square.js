function makeSquare(fill){
    const edge = 80;
    const offset = 100-edge;
    return circleHtml = `<circle x="${offset}" y="${offset}" width="${edge}" height="${edge}" stroke="green" stroke-width="4" fill="${fill}" />`
}

module.exports = makeSquare;