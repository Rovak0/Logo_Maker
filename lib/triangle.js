function makeTriangle(fill){
    const edge = 80;
    const offset = 100-edge;
    return circleHtml = `<polygon points="0,100 50,0, 100,100" stroke="green" stroke-width="4" fill="${fill}" />`
}

module.exports = makeTriangle;